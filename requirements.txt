# EchoDrive — pinned dependencies for Python 3.7 (64-bit) on Windows (win_amd64).
#
# Versions are pinned to the newest releases that (a) support Python 3.7 and
# (b) were available on PyPI when this study environment was set up.
#
# Install with:  pip install -r requirements.txt

# --- CARLA Python API (must match the CARLA 0.9.13 simulator) ---
carla==0.9.13

# --- ScenarioRunner 0.9.13 dependencies ---
py-trees==0.8.3
networkx==2.2
Shapely==1.7.1
psutil==7.0.0
xmlschema==1.0.18
ephem==4.2
tabulate==0.9.0
numpy==1.21.6
matplotlib==3.5.3
six==1.17.0
simple-watchdog-timer==0.1.1

# --- Game, audio, and input ---
pygame==2.6.1
PyOpenAL==0.7.11a1
keyboard==0.13.5

# --- Gesture recognition (webcam hand tracking) ---
# MediaPipe 0.8.11 for Python 3.7 / win_amd64, bundled as a local wheel (this exact build is no longer on PyPI).
./mediapipe-0.8.11-cp37-cp37m-win_amd64.whl
opencv-python==4.11.0.86
# Pulled in by mediapipe; pinned so it cannot drift to OpenCV 5.x.
opencv-contrib-python==4.11.0.86

# --- Data logging ---
pandas==1.3.5
openpyxl==3.1.3
# Pulled in by pandas
pytz==2025.2
